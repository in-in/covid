- let s = require(`../styles/styles.${env}.json`);
- let tokens = require('../tokens/build/tokens.json');
- let d = require('../data/data.json');

<!DOCTYPE html>
html(lang='en' dir='ltr')
	head
		include ../head/index.pug
	body(class=s.layout)
		block body

		each js in htmlWebpackPlugin.files.js
			if js.includes('main')
				script(src= js)
